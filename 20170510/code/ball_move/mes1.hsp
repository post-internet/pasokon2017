dim tana,4 ;dim「配列の名前,「配列の個数」

tana(0)=11 ;
tana(1)=4  ;それぞれの配列に中身を入れる
tana(2)=5  ;
tana(3)=14 ; 

mes(tana(0)) ;0番の配列の値を表示
mes(tana(1)) ;1番の配列の値を表示
mes(tana(2)) ;2番の配列の値を表示
mes(tana(3)) ;3番の配列の値を表示